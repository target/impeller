name: my-cluster-name
helm:
  repos:
    - name: private-repo
      url: https://example.com/my-private-repo/
      username:
        value: myuser
      password:
        valueFrom:
          environment: "unittest-password"
